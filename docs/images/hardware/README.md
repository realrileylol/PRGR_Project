# Hardware images

Drop hardware photos / CAD renders here and reference them from the docs.

## Expected files

| Filename | What it is | Referenced by |
|---|---|---|
| `tower_render.png` | CAD render of the two-bay radar + electronics test tower | `PRGR_Complete_Briefing.md` |

To add the tower render: save the CAD screenshot as **`tower_render.png`** in this folder,
then commit it. The docs already reference this path, so the image will appear automatically
once the file is committed.
